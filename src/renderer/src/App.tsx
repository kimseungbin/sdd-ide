// Root component for the empty shell (BL-001). The real UI — a component system
// on headless primitives with a closed variant API (see CLAUDE.md) — arrives with
// the React UI foundation (BL-033) in M3.
export function App() {
  return <main>SDD IDE — empty shell (BL-001), now on React.</main>
}
